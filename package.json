{
  "name": "monorepo",
  "version": "1.4.0",
  "private": true,
  "workspace_*": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{js,jsx,json,ts,tsx,md}\"",
    "test": "turbo test"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
